import 'dart:ui';

/// 색상 클래스
class CommonColors {
  // 공통
  static const transparent = Color(0x00000000); // 투명
  static const white = Color(0xFFFFFFFF);       // 흰색
  static const black = Color(0xFF000000);       // 검은색

  // 글자색
  static const Color txtWhite = Color(0xFFFFFFFF);
  static const Color txtBlack = Color(0xFF000000);

  // 배경색
  static const Color bgWhite = Color(0xFFFFFFFF);
  static const Color bgBlack = Color(0xFF000000);

  // Toast
  static const Color bgToast = Color(0xFF273646);
}